const authController = require('server/controllers/authenticationController')

describe('testing password encryption', () => {

  const next = jest.fn()

  beforeEach(() => {
    const req = {
      body: {},
      params: {},
    }
    const res = {
      locals: {}
    }
  })
  
  describe('')
  it('', () => {
      
  })
})